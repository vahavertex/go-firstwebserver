# Разница 1: Инициализация шаблонов (template.Must vs main)
* В Варианте Б: var tmpl = template.Must(template.ParseFiles(...)) на уровне пакета.
* В Варианте А: Объявление переменной, а сам парсинг и обработка ошибки происходят внутри main().

## Вот полностью обновленная структура проекта с разделением на пакеты по стандартам Go 1.26 без использования embed.
### 1. Структура проекта
''' text
├── go.mod (внутри директива: go 1.26)
├── main.go
├── templates/
│   └── index.html
└── internal/
    └── server/
        └── server.go 
'''